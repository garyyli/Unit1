#Gary Li
#9/6/17
#warmup1.py - finding the are of triangle

base = float(input('Enter the base of the triangle: '))
height = float(input('Enter the height of the triangle: '))
print('The area of the triangle is', (base*height)/2)